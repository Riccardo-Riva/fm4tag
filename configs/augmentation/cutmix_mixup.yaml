# Single view: CutMix on raw features, then Mixup in latent space.
views:
  - raw:
      - _target_: fm4tag.augmentations.CutMix
        lam: 0.1
    latent:
      - _target_: fm4tag.augmentations.Mixup
        lam: 0.1
