# Single-GPU trainer settings (no DDP strategy overhead)
max_epochs:         100
accelerator:        gpu
devices:            1
strategy:           auto
precision:          "bf16-mixed"
log_every_n_steps:  50
gradient_clip_val:  1.0
